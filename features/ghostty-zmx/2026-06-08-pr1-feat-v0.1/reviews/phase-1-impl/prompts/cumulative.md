# Cumulative implementation review

Review the current full diff from `main..HEAD` against the canonical spec and the current tracker.

Use your base lens prompt plus this cumulative prompt. The review should consider all implementation commits, not just the most recent fixer pass.

Write your report to the path specified by the orchestrator.

Cite file:line for every block/high/medium finding. Include well-maintained areas and a concise summary.
