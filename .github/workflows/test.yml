name: Test

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  shell-tests:
    runs-on: macos-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Ensure jq
        run: command -v jq || brew install jq

      - name: Run tests
        run: zsh tests/ci.zsh
